{
  "schema_version": "smarthaus.security-scanning-policy.v1",
  "required_gates": [
    "secret-scan",
    "trufflehog",
    "semgrep"
  ],
  "semgrep_config": ".semgrep.yml",
  "gitleaks_config": ".gitleaks.toml",
  "secret_scanners": [
    "gitleaks",
    "trufflehog"
  ],
  "github_code_security": {
    "codeql": "enable when GHAS/default setup is available",
    "secret_scanning": "enable when GitHub Code Security is available",
    "push_protection": "enable when GitHub Code Security is available"
  },
  "failure_mode": "fail_closed"
}
